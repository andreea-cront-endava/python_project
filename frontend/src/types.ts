export interface PowerRequest { base: number; exponent: number; }
export interface FibRequest   { n: number; }
export interface FactRequest  { n: number; }
export interface ResultResponse { result: number; }
